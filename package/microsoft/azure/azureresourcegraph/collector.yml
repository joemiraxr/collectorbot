classes:
    # Name of schema artifact that contains the classes below
    - "@zerobias-org/schema-microsoft-azure-azureresourcegraph":
        # Azure Resource Graph `Resources` table rows (listResources)
        - AzureResourceGraphResource
        # Azure Resource Graph `ResourceContainers` table rows
        # (listResourceContainers), one concrete class per row kind
        - AzureResourceGraphSubscription
        - AzureResourceGraphResourceGroup
        - AzureResourceGraphManagementGroup
